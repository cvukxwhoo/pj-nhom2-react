import classNames from "classnames/bind";
import styles from "./BoxPartner.module.scss";

const cx = classNames.bind(styles);

const BoxPartner = () => {
  return <div></div>;
};

export default BoxPartner;
